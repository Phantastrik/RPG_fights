function showStartScreen() {
    clearCanvas();
    ctx.font = "20px Arial";
    ctx.fillText("Choisissez un personnage pour commencer", 100, 100);
    
    // Ajouter les boutons ou les choix de personnage
    // Appel de l'initialisation de la Run une fois le personnage sélectionné
}
